{
  "name": "pg-notify",
  "version": "1.0.0",
  "description": "Postgres pg notify subscription system",
  "main": "./src/index.js",
  "repository": "git@github.com:becual/node-pg-notify.git",
  "author": "Becual Technologies S.A <tecnologia@becual.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "db:up": "docker-compose up -d db",
    "db:stop": "docker-compose stop db",
    "test": "node -r dotenv/config node_modules/.bin/jest --watchAll --runInBand"
  },
  "dependencies": {
    "chalk": "^2.3.2",
    "ramda": "^0.25.0"
  },
  "devDependencies": {
    "dotenv": "^5.0.1",
    "jest": "^22.4.2"
  },
  "jest": {
    "coveragePathIgnorePatterns": [
      "./node_modules/",
      "./output/",
      "./storage/"
    ],
    "testPathIgnorePatterns": [
      "./node_modules/",
      "./output/",
      "./storage/"
    ],
    "watchPathIgnorePatterns": [
      "./node_modules/",
      "./output/",
      "./storage/"
    ],
    "modulePathIgnorePatterns": [
      "./node_modules/",
      "./output/",
      "./storage/"
    ]
  },
  "peerDependencies": {
    "pg": "^7.4.1"
  }
}
